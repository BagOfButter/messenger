export type RegisterFormInput = {
    email: string;
    username: string;
    password: string;
    repeatPassword: string;
};

export type RegisterInfo = {
    email: string;
    username: string;
    password: string;
};